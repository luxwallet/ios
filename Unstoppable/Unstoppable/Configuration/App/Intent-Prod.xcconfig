PRODUCT_BUNDLE_IDENTIFIER = network.lux.wallet.intent
PROVISIONING_PROFILE_SPECIFIER[config=Debug-Prod] = match Development network.lux.wallet.intent
PROVISIONING_PROFILE_SPECIFIER[config=Release-Prod] = match AppStore network.lux.wallet.intent
