#include "../Config.xcconfig"

PRODUCT_BUNDLE_IDENTIFIER = network.lux.wallet
APP_DISPLAY_NAME = Lux Wallet
CODE_SIGN_ENTITLEMENTS = Unstoppable/Configuration/App/App-Prod.entitlements
PROVISIONING_PROFILE_SPECIFIER[config=Debug-Prod] = match Development network.lux.wallet
PROVISIONING_PROFILE_SPECIFIER[config=Release-Prod] = match AppStore network.lux.wallet
ASSETCATALOG_COMPILER_APPICON_NAME = AppIcon

SHARED_CLOUD_CONTAINER_ID = iCloud.network.lux.wallet.shared
PRIVATE_CLOUD_CONTAINER_ID = iCloud.network.lux.wallet
